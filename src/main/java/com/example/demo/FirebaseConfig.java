package com.example.demo;

import org.springframework.context.annotation.Configuration;

@Configuration
public class FirebaseConfig {
    // Firebase is now accessed via REST API directly
    // No SDK initialization needed
}